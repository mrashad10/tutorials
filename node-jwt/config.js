javascript;
/**
 * @module config
 * @description This module contains the configuration settings for the
 * application.
 */

/**
 * @type {Object}
 * @property {string} secret - The secret key used for authentication.
 */
const config = {
  secret: "supersecret",
};

module.exports = config;
